#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 97; a < 101; a++)
	{
		putchar(a);
	}
	for (a = 102; a < 113; a++)
	{
		putchar(a);
	}
	for (a = 114; a < 123; a++)
	{
		putchar(a);
	}
		putchar('\n');
	return (0);
}
